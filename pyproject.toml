[project]
name = "vet-rag-api"
version = "0.1.0"
description = "Veterinary RAG ingestion and retrieval API"
readme = "README.md"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"
